---
interface Props {
	id?: string;
	title: string;
}

const { id, title } = Astro.props;
---

<section {id} class="border-t-8 px-5 py-10 sm:px-10 md:px-15 md:py-14">
	<h2 class="font-display font-bold text-3xl sm:text-4xl uppercase mb-6 mb-8">{title}</h2>
	<slot />
</section>
